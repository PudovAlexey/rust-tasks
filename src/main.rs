pub mod tasks;

use tasks::{
   // find_all_anagrams_in_string,
   make_linked_list,
   Merge_k_sorted_lists,
   two_sum,
   roman_numerals_encoder,
   highest_scoring_word,
};

fn main() {
   // let result =  find_all_anagrams_in_string::find_all_anagrams_in_string("cbaebabacd", "abc");

   // let link_data: Vec<&str> = Vec::from(["a", "b", "c", "d"]);

   // let link_list = make_linked_list::make_linked_list(link_data);
   // Merge_k_sorted_lists::Merge_k_sorted_lists();

   // println!("{:?}", link_list);

   // let res = two_sum::two_sum(&[1, 2, 3], 10);

   // println!("{:?}", res);

   // let roman = roman_numerals_encoder::roman_numerals_encoder(44);
   // let highest_scoring_word = highest_scoring_word::highest_scoring_word("aa b");
}